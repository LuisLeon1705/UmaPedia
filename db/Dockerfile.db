FROM postgres:16-alpine

ENV POSTGRES_USER=uma_user
ENV POSTGRES_PASSWORD=uma_password
ENV POSTGRES_DB=uma_db

# Initialize database schema on first container startup
COPY schema.sql /docker-entrypoint-initdb.d/schema.sql
